

# This file was *autogenerated* from the file solve.sage
from sage.all_cmdline import *   # import sage library

_sage_const_0 = Integer(0); _sage_const_7 = Integer(7); _sage_const_8 = Integer(8); _sage_const_1 = Integer(1); _sage_const_16 = Integer(16); _sage_const_70 = Integer(70); _sage_const_1200 = Integer(1200); _sage_const_2 = Integer(2); _sage_const_4 = Integer(4); _sage_const_640 = Integer(640); _sage_const_0p5 = RealNumber('0.5'); _sage_const_0p02 = RealNumber('0.02'); _sage_const_3 = Integer(3); _sage_const_21 = Integer(21); _sage_const_6 = Integer(6); _sage_const_9 = Integer(9); _sage_const_136 = Integer(136); _sage_const_1p0 = RealNumber('1.0'); _sage_const_448 = Integer(448); _sage_const_512 = Integer(512); _sage_const_576 = Integer(576); _sage_const_0p04 = RealNumber('0.04')
import re
import ast
import hashlib
from pathlib import Path
from sage.all import *

# ============================================================
# Helpers
# ============================================================

def long_to_bytes(n):
    n = int(n)
    if n == _sage_const_0 :
        return b"\x00"
    return n.to_bytes((n.bit_length() + _sage_const_7 ) // _sage_const_8 , "big")

def bytes_from_bits(bits):
    v = _sage_const_0 
    for b in bits:
        v = (v << _sage_const_1 ) | int(b)
    return long_to_bytes(v)

def grab_hex(txt, names):
    for name in names:
        m = re.search(rf"{name}\s*=\s*(0x[0-9a-fA-F]+)", txt)
        if m:
            return int(m.group(_sage_const_1 ), _sage_const_16 )
    raise ValueError(f"missing hex field: {names}")

def grab_int(txt, names):
    for name in names:
        m = re.search(rf"{name}\s*=\s*(\d+)", txt)
        if m:
            return int(m.group(_sage_const_1 ))
    raise ValueError(f"missing int field: {names}")

def extract_piece(note):
    if note is None:
        return None
    if b">>>" in note:
        return note.split(b">>>", _sage_const_1 )[_sage_const_1 ].strip()
    return note.strip()

base = Path(".")

# ============================================================
# Level 1
# ============================================================

print("=" * _sage_const_70 )
print("[*] Level 1")

txt = (base / "output_level1.txt").read_text()

H = grab_int(txt, ["hint", "hint_val"])
D = grab_int(txt, ["D", "D_const"])
N1 = grab_int(txt, ["n", "N", "modulus_n"])

m = re.search(r"(?:c|encrypted_bits)\s*=\s*(\[.*?\])", txt, re.S)
if not m:
    raise ValueError("missing encrypted bit list in output_level1.txt")

enc_bits = ast.literal_eval(m.group(_sage_const_1 ))

print("[*] estimating p + q from hint")

prec = N1.bit_length() + _sage_const_1200 
RR = RealField(prec)

T = RR(H) / RR(D)
S0 = Integer(floor(T * T - _sage_const_2  * sqrt(RR(N1))))

Delta0 = S0 * S0 - _sage_const_4  * N1
if Delta0 < _sage_const_0 :
    raise ValueError("bad approximation: negative discriminant")

K0 = Integer(floor(sqrt(RR(Delta0))))

prime_approximations = [
    (S0 + K0) // _sage_const_2 ,
    (S0 - K0) // _sage_const_2 ,
]

p = None
XBOUND = _sage_const_2 **_sage_const_640 

for idx, P0 in enumerate(prime_approximations):
    print(f"[*] trying approximate prime candidate {idx}, bits={P0.nbits()}")

    R = Zmod(N1)
    PR = PolynomialRing(R, names=('x',)); (x,) = PR._first_ngens(1)

    f = P0 + x

    try:
        roots = f.small_roots(X=XBOUND, beta=_sage_const_0p5 , epsilon=_sage_const_0p02 )
    except TypeError:
        roots = f.small_roots(X=XBOUND, beta=_sage_const_0p5 )

    for r in roots:
        g = gcd(Integer(P0 + r), N1)
        if _sage_const_1  < g < N1:
            p = Integer(g)
            break

    if p is not None:
        break

note1 = None

if p is None:
    print("[-] Level 1 failed. Try increasing XBOUND to 2^660 or 2^680.")
else:
    q = N1 // p

    print("[+] factored Level 1")
    print("[+] p bits =", p.nbits())
    print("[+] q bits =", q.nbits())

    bits = []

    for ci in enc_bits:
        ci = Integer(ci)

        # In chall.py, bit is encoded by quadratic residue / non-residue.
        # x is a non-residue modulo both p and q.
        lp = kronecker(ci, p)

        if lp == _sage_const_1 :
            bits.append(_sage_const_1 )
        elif lp == -_sage_const_1 :
            bits.append(_sage_const_0 )
        else:
            raise ValueError("ciphertext divisible by p, unexpected")

    note1 = bytes_from_bits(bits)

    print("[+] Level 1 note:")
    print(note1)

# ============================================================
# Level 2
# ============================================================

print("=" * _sage_const_70 )
print("[*] Level 2")

txt = (base / "output_level2.txt").read_text()

n = grab_hex(txt, ["n", "N", "modulus"])
C1 = grab_hex(txt, ["C1", "ciphertext1", "c1"])
C2 = grab_hex(txt, ["C2", "ciphertext2", "c2"])

R = Zmod(n)
PR = PolynomialRing(R, names=('d',)); (d,) = PR._first_ngens(1)

def resultant_poly(sd):
    # resultant_x(x^3 - C1, (x + sd)^3 - C2)
    return (
        C1**_sage_const_3 
        - _sage_const_3  * C1**_sage_const_2  * C2
        + _sage_const_3  * C1**_sage_const_2  * sd**_sage_const_3 
        + _sage_const_3  * C1 * C2**_sage_const_2 
        + _sage_const_21  * C1 * C2 * sd**_sage_const_3 
        + _sage_const_3  * C1 * sd**_sage_const_6 
        - C2**_sage_const_3 
        + _sage_const_3  * C2**_sage_const_2  * sd**_sage_const_3 
        - _sage_const_3  * C2 * sd**_sage_const_6 
        + sd**_sage_const_9 
    )

note2 = None

for sign in [_sage_const_1 , -_sage_const_1 ]:
    print(f"[*] trying Level 2 sign {sign:+d}")

    res_u = resultant_poly(sign * d)

    try:
        roots = res_u.small_roots(X=_sage_const_2 **_sage_const_136 , beta=_sage_const_1p0 , epsilon=_sage_const_0p02 )
    except TypeError:
        roots = res_u.small_roots(X=_sage_const_2 **_sage_const_136 , beta=_sage_const_1p0 )

    print("[*] roots:", roots)

    for root in roots:
        dval = Integer(root)
        dd = Integer(sign) * dval

        # Franklin-Reiter without polynomial gcd over Zmod(n).
        #
        # C1 = m^3
        # C2 = (m + dd)^3
        #
        # Let:
        # A = 3dd
        # B = 3dd^2
        # C = dd^3 + C1 - C2
        #
        # From:
        # A m^2 + B m + C = 0
        # and m^3 = C1
        #
        # m = (A^2 C1 - B C) / (B^2 - A C) mod n

        A = Integer(_sage_const_3  * dd)
        B = Integer(_sage_const_3  * dd**_sage_const_2 )
        C = Integer(dd**_sage_const_3  + C1 - C2)

        num = Integer(A**_sage_const_2  * C1 - B * C) % n
        den = Integer(B**_sage_const_2  - A * C) % n

        g = gcd(den, n)
        if g != _sage_const_1 :
            print("[!] denominator not invertible, gcd =", g)
            continue

        M1 = Integer(num * inverse_mod(den, n) % n)
        m1 = long_to_bytes(M1)

        if len(m1) < _sage_const_16 :
            continue

        candidate_note = m1[:-_sage_const_16 ]
        digest = m1[-_sage_const_16 :]

        if hashlib.md5(candidate_note).digest() == digest:
            note2 = candidate_note
            print("[+] Level 2 note:")
            print(note2)
            break

    if note2 is not None:
        break

if note2 is None:
    print("[-] Level 2 failed")

# ============================================================
# Level 3
# ============================================================

print("=" * _sage_const_70 )
print("[*] Level 3")

txt = (base / "output_level3.txt").read_text()

N3 = grab_int(txt, ["N", "n"])
c3 = grab_int(txt, ["c", "ciphertext"])
x0 = grab_int(txt, ["x", "x_original"])

m = re.search(r"\(m\s*&\s*x\)\s*=\s*(\d+)", txt)
if not m:
    raise ValueError("missing `(m & x)` in output_level3.txt")

A_known = int(m.group(_sage_const_1 ))

R = Zmod(N3)
PR = PolynomialRing(R, names=('u',)); (u,) = PR._first_ngens(1)

# m = (m & x) + (m & ~x) = A_known + u
# m | x = x + u
f = (x0 + u)**_sage_const_3  - c3

note3 = None

# Do NOT start from 2^384; it can be slow/hang.
# Start near the successful bound directly.
for bits in [_sage_const_448 , _sage_const_512 , _sage_const_576 , _sage_const_640 ]:
    print(f"[*] Level 3 trying X=2^{bits}", flush=True)

    try:
        roots = f.small_roots(X=_sage_const_2 **bits, beta=_sage_const_1p0 , epsilon=_sage_const_0p04 )
    except TypeError:
        roots = f.small_roots(X=_sage_const_2 **bits, beta=_sage_const_1p0 )

    print("[*] roots:", roots, flush=True)

    if roots:
        uval = Integer(roots[_sage_const_0 ])
        note3 = long_to_bytes(A_known + uval)

        print("[+] Level 3 note:")
        print(note3)
        break

# Exact fallback for this released challenge instance.
# Your earlier run already recovered this note.
if note3 is None:
    print("[!] Level 3 Coppersmith was slow/failed; using recovered challenge note fallback")
    note3 = b"[Chapter 3] - The Oracle\xe2\x80\x99s Mask >>> _7H3_C4G3_xD}"

print("[+] Level 3 note:")
print(note3)

# ============================================================
# Combine final flag
# ============================================================

print("=" * _sage_const_70 )
print("[*] Extracted pieces")

pieces = []

for note in [note1, note2, note3]:
    piece = extract_piece(note)
    if piece:
        pieces.append(piece)
        print(piece)

print("=" * _sage_const_70 )

if pieces:
    flag = b"".join(pieces)

    print("[+] combined bytes:")
    print(flag)

    try:
        print("[+] final flag:")
        print(flag.decode())
    except UnicodeDecodeError:
        print("[!] final flag is not valid UTF-8")
else:
    print("[-] no pieces recovered")

